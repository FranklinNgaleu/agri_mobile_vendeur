import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'front_vendeur',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
